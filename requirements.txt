# Azure Functions runtime
azure-functions

# Azure Table Storage client — used for conversationId deduplication
# and storing the Graph subscription ID
azure-data-tables

# Azure Identity — provides ClientSecretCredential for Graph OAuth token acquisition
azure-identity

# HTTP client — used for all Graph API and Affinity API calls
requests

# Linting and formatting tools
flake8
black